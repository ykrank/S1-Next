<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Example customization of dimensions originally defined in res/values/dimens.xml
         (such as screen margins) for screens with more than 820dp of available width. This
         would include 7" and 10" devices in landscape (~960dp and ~1280dp respectively). -->
    <dimen name="activity_horizontal_margin">64dp</dimen>

    <!--Before L, CardView adds padding to its content and draws shadows to that area.
    This padding amount is equal to maxCardElevation + (1 - cos45) * cornerRadius on the
    sides and maxCardElevation * 1.5 + (1 - cos45) * cornerRadius on top and bottom.-->
    <!--<dimen name="card_view_margin">32dp</dimen>-->

    <!-- 32dp (cardView margin) - ((2 + (1 - cos45) * 2))dp ≈ 29.05dp -->
    <dimen name="card_view_horizontal_margin">29.05dp</dimen>
    <!-- 16dp (half of cardView margin) - (2 * 1.5 + (1 - cos45) * 2)dp ≈ 12.05dp -->
    <dimen name="card_view_vertical_margin">12.05dp</dimen>
    <!-- 32dp (cardView margin) - (2 * 1.5 + (1 - cos45) * 2)dp = 28.05dp -->
    <dimen name="recycler_view_padding_for_card">28.05dp</dimen>

</resources>
