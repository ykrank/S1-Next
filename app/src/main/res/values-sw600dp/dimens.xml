<?xml version="1.0" encoding="utf-8"?>
<resources>

    <dimen name="activity_horizontal_padding">24dp</dimen>
    <dimen name="activity_vertical_padding">20dp</dimen>

    <dimen name="recycler_view_horizontal_padding">24dp</dimen>
    <dimen name="recycler_view_vertical_padding">12dp</dimen>

    <!-- 24dp (CardView margin) - ((2 + (1 - cos45) * 2))dp ≈ 21.05dp -->
    <dimen name="card_view_horizontal_margin">21.05dp</dimen>
    <!-- 6dp (half of CardView margin) - (2 * 1.5 + (1 - cos45) * 2)dp ≈ 2.05dp -->
    <dimen name="card_view_vertical_margin">2.05dp</dimen>
    <!-- 12dp (CardView margin) - (2 * 1.5 + (1 - cos45) * 2)dp - 2.05dp (CardView vertical margin) = 6dp -->
    <dimen name="recycler_view_padding_for_card">6dp</dimen>

</resources>
